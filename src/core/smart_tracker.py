# SmartTracker has been removed. This stub preserves the module path
# so any lingering references produce a clear error rather than an ImportError.


class SmartTracker:
    """Removed — all tracking logic has been eliminated in favour of direct detection coordinates."""

    def __init__(self, *args, **kwargs):
        raise RuntimeError("SmartTracker has been removed. Set tracker_enabled=False in config.")
