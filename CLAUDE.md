# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git Workflow

- **Main is branch-protected** — never push directly to `main`. Always develop on a feature branch and open a PR via `mcp__github__create_pull_request` targeting `chr0mx/axiom-ai-aimbot`.
- Branch naming convention in use: `claude/<topic>-<id>`
- After any rebase, re-author commits: `git config user.email noreply@anthropic.com && git config user.name Claude && git rebase --exec "git commit --amend --no-edit --reset-author" origin/main`

## Running Tests

```bash
# All tests
pytest tests/

# Single test file
pytest tests/test_inference.py

# Single test
pytest tests/test_config.py::TestConfigInit::test_screen_dimensions
```

Tests add `src/` to `sys.path` via `tests/conftest.py` — no install step needed.

**On non-Windows (e.g. this sandbox)**: this is fundamentally a Windows app — `pytest tests/` reports a large number of environment-only failures (missing `win32api`/PyQt6/pywin32, not code bugs); this baseline shifts as tests are added/removed, so when verifying a change doesn't regress anything, compare the failed/passed counts against a fresh baseline run rather than a hardcoded number. Any module with a top-level `import win32api` (or that transitively imports one, e.g. anything importing `win_utils`) fails at **collection**, not just at test-run time, which aborts the whole suite unless the import is deferred — the established pattern here is a pytest fixture (or an in-test `from ... import ...`) that does the import lazily, so a missing `win32api` fails just that file's tests individually instead of blocking every other test file's collection. `ai_loop.py`, `ai_aiming.py`, `auto_fire.py`, and `key_listener.py` all hit this (the first three via `win_utils`, `key_listener.py` directly); `ai_loop_utils.py` defers its `import win32api` inside a function, so it's collectible even though it still fails at test-run time. `app_controller.py` follows the same pattern (its `win_utils`/`onnxruntime`/`.ai_loop`/`.auto_fire`/`.session_utils` imports are all deferred inside the functions that need them), so `set_always_aim()` is testable here even though `start_ai_threads()` etc. still aren't callable. `detection_semantics.py`, `udp_receiver.py`, and `esp_server.py` don't hit either problem and are fully testable here.

## Architecture

### Entry Point
`src/main.py` — sets DPI awareness (`SetProcessDpiAwarenessContext` Per-Monitor V2 with fallbacks), injects `src/`, `src/python/dependencies/`, and PyWin32 paths into `sys.path`, pre-loads native DLLs, initialises logging, selects ONNX backend (DirectML / CUDA / TensorRT / CPU), then starts the Qt GUI.

`src/version.py` (`__version__`) is the single source of truth for the app version — read by `src/core/updater.py` (GitHub release check) and the About/version label in `other_page.py`; don't hardcode version strings elsewhere.

### Inference Pipeline (three threads)
The main loop (`src/core/ai_loop.py`, `ai_logic_loop()`) runs three concurrent workers:

1. **`_capture_worker`** — calls the active backend's `grab()` at `screenshot_interval`, writes frames into `capture_state['latest_frame']` under `frame_lock`. Also calls `set_preview_frame()` for the live preview panel. Hot-swaps capture backends every 0.5 s via `reinitialize_if_method_changed`.
2. **`_preprocess_worker`** — reads frames, letterboxes/resizes (or fast-path for square captures), builds the ONNX input tensor, pushes to `_tensor_queue`.
3. **Main inference thread** — pulls tensors, runs `model.run()` (or `run_with_iobinding()` for CUDA IO binding), calls `postprocess_outputs()` + NMS, then `process_aiming()`. Supports hot-swap of models and providers at runtime (`_try_hot_swap_model`).

Frame capture and inference are fully decoupled — capture backends must return `(H, W, 4)` uint8 BGRA.

### Screen Capture Backends (`src/core/screen_capture.py`)
Five backends share a common `grab(region) → np.ndarray | None` interface:
- **MSS** — GDI32, cross-platform fallback
- **dxcam** — Desktop Duplication API (GPU, lowest latency; returns `None` on no-change — caller reuses last valid frame)
- **UVCCapture** — OpenCV `VideoCapture` with a dedicated `_reader_thread` so `grab()` never blocks; supports probed FPS, MSMF/V4L2 backends, always-on-top pop-out preview. `uvc_capture_method == 'dshow'` additionally selects between two DirectShow implementations via `uvc_dshow_backend`: `'v1'` (default) is `cv2.VideoCapture(CAP_DSHOW)`; `'v2'` is the native `directshow_capture.dll` (`chr0mx/DirectShow-Capture-DLL`, vendored at `src/python/dependencies/`) via `src/core/dshow_capture_native.py`'s ctypes binding (`NativeDshowCapture`) — owns the capture graph's allocator/buffer count directly instead of going through OpenCV, for lower/OBS-like latency. Both MJPEG and NV12 are supported under v2 (`uvc_video_format`, restricted to those two in the GUI while v2 is active — `yuy2`/`yuv420p` have no v2 equivalent); MJPEG is decoded to BGR in `_reader_worker_native_dll`, NV12 reuses the same raw-NV12 crop-before-convert path (`_crop_nv12`) the cv2 path's `CAP_PROP_CONVERT_RGB` optimization already uses. `uvc_ffmpeg_enabled` (external `ffmpeg.exe` subprocess) is gated to `dshow` + `v1` only — ffmpeg has no MSMF demuxer on Windows, and v2 already owns the graph directly. `uvc_crop_mode == 'fixed'` (vs. the default `'dynamic'`, which recomputes the crop from the live Detection Range every frame) freezes a centered `detect_range_size` square as the capture region; on v2/NV12 this is requested as a genuine native crop via the DLL's `dsc_open_params.crop_x/y/width/height` (`_compute_fixed_uvc_crop_region()`, shared with the cv2 path) — **not** by asking the device to negotiate a smaller resolution, which carries no guarantee of being a spatial crop of the same framing rather than the whole scene rescaled. Device negotiation always requests the full `uvc_width`/`uvc_height` regardless of crop mode; the crop is a separate, later, in-DLL step. Because a DLL build predating this crop ABI addition silently ignores the trailing crop fields instead of rejecting them, `_init_native_dll` treats `capture_open()` succeeding as provisional, not proof — `_reader_worker_native_dll` confirms the crop actually took effect against the first real frame's delivered dimensions and self-heals to a software crop (`_fixed_region`) if the DLL didn't honor it. Native crop is NV12-only (MJPEG would need decode+crop+re-encode, which the DLL doesn't implement); MJPEG's fixed-crop mode always uses the software path.
- **NDICapture** — cyndilib frame-sync path (UYVY → BGRA via `cv2.COLOR_YUV2BGRA_UYVY`); uses buffer protocol (`np.frombuffer`) for zero-copy frame access; auto-reconnects on source loss
- **UdpCapture** — receives an MJPEG-over-UDP stream matching OBS's `udp_stream_filter` plugin wire format (`src/core/udp_receiver.py`: 14-byte chunk header, frame reassembly, stale-frame timeout); decodes JPEG chunks into BGRA frames

The module also owns the **live preview cell**: `set_preview_frame()` / `get_preview_frame()` / `set_preview_region()` / `get_preview_region()` expose the current detection-region frame to the GUI preview panel under a single `_preview_lock`.

### Config (`src/core/config.py`)
Single `Config` class — all runtime state lives here. Persistence is driven by `_FIELD_MAP`, a module-level dict mapping every flat `Config` attribute to its dotted path in the **v2 grouped JSON schema** (e.g. `'fov_size': 'aim.fov_size'`). `to_dict()` and `from_dict()` are both generated from `_FIELD_MAP`; adding a new persisted field only requires one entry there plus the `__init__` assignment.

**Three separate persistence targets:**
- `config.json` — all fields in `_FIELD_MAP` (v2 nested schema)
- `state.json` — one-time app state in `STATE_FIELDS` (`disclaimer_agreed`, `first_run_complete`, `ndi_installer_ran_once`)
- `language.json` — current UI language selection

`config_manager.py` owns load/save lifecycle, preset management, and migration from v1 flat format. Presets are full config snapshots saved as JSON to `config/*.json` (derived from `_FIELD_MAP`, so they can't drift from `Config`'s current field set); `src/core/presets/*.json` holds bundled built-in presets that get seeded into the user's `config/` dir on first run (never overwriting a same-named file the user already has). Preset names are sanitized (`_sanitize_config_name()`) before being interpolated into a file path — they can come from free-text GUI dialogs or an imported file's own `name` field, both untrusted.

**`ConfigManager.preview_config_changes(config_instance, config_name)`** — a dry run of `load_config()` used by `configs_page.py`'s Load button to show what a preset would actually change before applying it, instead of silently overwriting. Implemented as `copy.deepcopy(config_instance)` → `simulated.from_dict(preset_data)` (the exact code path a real load takes) → diff `_get_config_data()`'s output (the same flat, attr-keyed dict `save_config()`/`load_config()` already read and write) between the original and the simulated copy — so the preview can't drift from real load behavior, and needs no separate preset format of its own. Returns `[]` for "identical to current" (the GUI skips the confirmation prompt in that case and loads directly), `None` if the preset file couldn't be read (the GUI treats this the same as `load_config()` itself failing — falls through to the real call and lets its own error path report it), or a list of short change descriptions otherwise. `_describe_changed_attrs()` groups a handful of attrs that commonly change together (all 6 PID gains, every `makcu_*`/etc. field, `humanization`'s sub-fields) under one friendly "N values in `<group>`" line via a small prefix table (`_PRESET_DIFF_GROUPS`) — purely cosmetic, an attr missing from that table just falls back to its own prettified name instead of being grouped.

### Aiming & Anti-Recoil (`src/core/ai_aiming.py`)
`process_aiming()` is the per-frame entry point, called from `ai_loop.py` with the **full** FOV-filtered candidate list (never pre-reduced — see single-target note below). Key subsystems:
- **FOV filtering** (`filter_boxes_by_fov()`, `ai_loop_utils.py`) — the FOV is a rectangle by default (`fov_size` = width, `fov_height` = height; equal by default, i.e. a square), or an ellipse (a circle when width == height) with `fov_circle_filter_enabled` — `_ellipse_intersects_bbox()` does the ellipse/box test via an affine transform to a unit circle, shared verbatim by the in-game overlay's tracer-line/box-highlight passes (`overlay.py`), `screen_capture.py`'s `_draw_detection_overlay()` (the baked-into-the-frame preview annotations UVC/NDI/UDP fall back to, since the transparent Qt overlay can't render over those non-desktop sources the way it does for mss/dxcam — the one surface FOV-rectangle support originally missed, so a rectangular FOV visually looked square/circular for exactly those three backends even though the underlying filter was already correct), and the Web ESP client (`app.js`'s `ellipseIntersectsBbox()`, a hand-ported mirror since the browser can't import Python) so all four surfaces agree on what's "in FOV." `get_effective_detect_range_size()`/`_validate_detect_range_size()` clamp `detect_range_size`'s lower bound to `max(fov_size, fov_height)` so the (always-square) detection/capture region can contain the whole FOV rectangle, not just its width.
- **Reduce FOV on Active Target** (`fov_reduce_on_target_enabled`) — once a target is locked (`state.locked_box` non-`None`), `ai_loop_utils.compute_effective_fov()` shrinks both `fov_size` and `fov_height` to `fov_min_size_pct`% of their configured values (same percentage on each axis, so a rectangular/elliptical FOV keeps its aspect ratio while shrunk) for up to `fov_min_size_duration` seconds, so a random target entering the edge of the normally-larger FOV can't steal the lock mid-engagement. `state.fov_reduce_since` — the shrink window's start time — is set exactly once on the None→non-None edge of `locked_box`, not every frame a target stays locked (the same class of continuous-reset bug already fixed once for the MAKCU disengage delay: setting it unconditionally every frame would pin the elapsed time near zero forever and the duration could never expire). `fov_min_size_duration == 0` holds the shrink indefinitely while locked, matching this codebase's "0 = uncapped/off" convention elsewhere. Once the window *does* expire, `state.fov_reduce_expired` latches so the FOV stays at full size for the rest of the current lock — without it, "just expired" and "no window has ever started" are both indistinguishable states of `fov_reduce_since == 0.0` while `locked_box` is still non-`None`, so the very next frame would misread expiry as a fresh acquisition and re-arm the shrink immediately, producing an unbroken shrink/revert loop that (since the reverted frame lasts a single tick) looks exactly like the duration is never respected at all. `filter_boxes_by_fov()` takes an optional `fov_height` override parameter for this — the shrunk height for just this call, without touching `config.fov_height` itself (which must stay the user's real configured value). The FOV actually in effect this frame is published every frame (not just while shrunk) as the runtime-only `config.fov_effective_size`/`fov_effective_height` (unpersisted, same pattern as `uvc_actual_width`) so all four FOV-drawing surfaces (`overlay.py`, `screen_capture.py`'s `_draw_detection_overlay()`, the Web ESP snapshot) visually shrink the FOV circle/rectangle in sync with target selection, rather than always drawing the static configured size — the one exception is each surface's own detect-range-size floor, which deliberately still reads the full configured `fov_size`/`fov_height`, not the momentary shrunk value, since that floor is tied to the user's real setting.
- **Target selection** — `target_priority_mode` (`'distance'` / `'confidence'` / `'composite'`, weighted by `target_priority_confidence_weight`) scores candidates; shared logic lives in `find_closest_target()` (`ai_loop_utils.py`), used both by `process_aiming()` and the idle-detect path in `ai_loop.py`
- **Sticky lock** — IOU-based target persistence across short detection gaps (`sticky_lock_enabled`), with an adaptive IOU threshold that scales with FOV size (`_adaptive_sticky_iou`)
- **`state.locked_box` / `state.locked_confidence`** (`ai_loop_state.py`) — written on *every* successful selection regardless of `sticky_lock_enabled`, so other code (e.g. `single_target_mode`'s box-list reduction) can read back the real post-lock pick instead of re-deriving a lock-blind one
- **`single_target_mode`** — reduces the box list Web ESP/auto-fire/preview see to just the locked target. This reduction happens **after** `process_aiming()` runs, derived from `state.locked_box`; it must never pre-filter the candidate list before sticky lock sees it, or sticky lock can never win over the frame's raw priority winner
- **Motion prediction** (`prediction_enabled`) — `src/core/target_predictor.py` (`VelocityPredictor`) or `src/core/kalman_filter.py` (`KalmanFilter2D`, constant-velocity 2D); both `.reset()` on true target loss to avoid stale velocity carrying into a new target
- **Semantic false-positive filter** (`detect_semantic_filter_enabled`) — `src/core/detection_semantics.py`; three layers (ONNX class-name allow/deny lists, aspect-ratio geometry heuristics, min bbox size) to drop vegetation/vehicle/sign/HUD misdetections before target selection
- **PID controller** with separate X/Y axes (`pid_kp_x/y`, `pid_ki_x/y`, `pid_kd_x/y`). Nothing downstream clamps Kp — the GUI's P sliders just cap their travel at a proven-stable 0.0–0.5 band by default; `pid_unsafe_mode` re-maps that same slider travel to the full 0.0–1.0 range ("Unsafe Mode" toggle, Aim page)
- **Y-axis recoil suppression** (`aim_y_reduce_*`) — delay, ramp, floor, settle gate, and velocity restore
- **Humanization** — post-processes the final PID-output (dx, dy) delta only (never detection, PID state, or coordinate mapping) via `src/core/humanization.py` (`HumanizationConfig` dataclass, `apply_humanization()`). GUI (Aim page) exposes a master Enable + Intensity (0–100%, scales every effect) plus one on/off toggle per feature (Micro-Jitter, Motion Variation, Speed Shaping, Micro-Stutter, Reaction Variability) and a fine-tuning slider for each feature's underlying sub-parameter (jitter base/scale, variation range, speed-shaping thresholds/factor, stutter probability/bounds, reaction skip probability) — each sub-slider is shown (and enabled) only while both the master switch and its own feature toggle are on — hidden rather than greyed out, to keep an all-features-off view uncluttered. A "Reset to Defaults" button restores the entire block (including Intensity and all feature toggles) to `HumanizationConfig()`'s dataclass defaults. `Config.humanization` persists the whole dataclass as its own `"humanization"` JSON block (a plain dict round-trip via `dataclasses.asdict()`, separate from the normal dotted-path `_FIELD_MAP` schema everything else uses) — so any field is also hand-editable directly in `config.json`/presets even without a GUI control.
  - **Idle Micro-Jitter** (`micro_jitter_idle_enabled`, off by default) — `process_aiming()` (and therefore the rest of Humanization) only ever runs on frames where `is_aiming and boxes` in `ai_loop.py`, so holding the aim key with no target detected normally leaves the crosshair perfectly still. When this toggle is on, `ai_aiming.apply_idle_micro_jitter()` is called instead from `ai_loop.py`'s `else` branch (whenever `is_aiming` is still True that frame) and reuses `apply_humanization(0.0, 0.0, hcfg)` unchanged — Speed Shaping/Motion Variation/Micro-Stutter all no-op on a zero-magnitude input via their own `magnitude > 0.0` guards, so only Micro-Jitter's amplitude floor fires, going through the same `state.aim_carry_x/y` sub-pixel carry as the on-target path. Gated separately from `micro_jitter_enabled` (which only covers the on-target case), so it's a genuine opt-in with no behavior change for existing configs/presets.
- **Deadzone** — minimum pixel gap before movement fires
- **Sub-pixel carry** (`state.aim_carry_x/y`) — accumulates the fractional remainder integer truncation discards each frame, applied universally across all mouse backends so micro-corrections are never silently lost

Auto-fire (`src/core/auto_fire.py`) consumes `config.latest_boxes` (the single-target-reduced list when applicable) via `update_queues(...)`.

### Secondary / Weapon Detection (process-isolated)
Both run in a separate child process so the main inference loop and Qt UI are never blocked, feeding results back via a small "feeder" thread. Selected via `second_inference_mode` (`'off'` / `'v1_ocr'` / `'v2_onnx'`) — the Inference page shows/hides the ONNX confidence threshold accordingly.
- **`src/core/ocr_inference.py`** — PaddleOCR-based weapon-name reader
- **`src/core/hud_inference.py`** — YOLO11n ONNX weapon/attachment detector. ROI read from `config.hud_roi_coords` (`"x1,y1,x2,y2"`, default `"1490,953,1870,1041"`, Apex Legends HUD strip @ 1080p), 320×320 letterboxed input. Public API: `start(config)`, `stop()`, `trigger_hud_scan()`, `get_hud_results()`, `get_hud_roi_image()`

### Web ESP Overlay (`src/core/esp_server.py` + `src/web_overlay/`)
Optional LAN-accessible browser overlay (`web_esp_enabled`), modeled as "backend streams state, frontend draws it" — the PyQt in-game overlay stays the on-device renderer. Three daemon threads:
- **HTTP server** (stdlib `ThreadingHTTPServer`) serves the static client from `src/web_overlay/` (`index.html` / `app.js` / `styles.css`); bare `/` requests without a `?ws=` query 302-redirect to the actual bound WS port so the client never needs a manually-appended port
- **WebSocket server** — hand-rolled RFC 6455 (handshake + text-frame encoding, no external dependency), auto-increments the bind port on conflict (`_actual_ws_port`)
- **Broadcast loop** — ~`web_esp_fps` Hz, latest-state-wins; serializes a JSON snapshot of `Config` (`_build_snapshot()`) to all connected clients, including 1 Hz-computed `capture_fps`/`inference_fps`

Reads `config.latest_all_boxes` / `latest_all_confidences` (unreduced by `single_target_mode` — same set the in-game overlay draws), not `config.latest_boxes`, so the web view always shows every detection regardless of aiming mode.

### Web Control API (`src/core/app_controller.py` + `src/core/web_control_server.py` + `src/web_control_client/`)
A second, separate LAN server (`web_control_enabled`) — a **control plane**, not read-only telemetry like Web ESP above, so it's built on a different stack and a different trust model:
- **`app_controller.py`** — the shared application-logic boundary a Qt slot and a web route both call into. Plain functions, `config` first, no Qt import anywhere in the call chain (same tier as `ai_loop.py`/`config_manager.py`/`session_utils.py`). Holds `start_ai_threads()` / `stop_ai_threads()` / `pause_ai_inference()` / `resume_ai_inference()` — moved verbatim out of `main.py` (same behavior, same `_ai_threads_lock`) specifically so something other than `main.py`'s own startup/shutdown code can call them — and `set_always_aim(config, enabled)`, extracted from `keys_page.py`'s `_onAlwaysAimChanged` (which used to inline the `always_aim`/`idle_detect_enabled` coupled write directly in a Qt slot; the decide-half now lives here, the slot just calls it then refreshes its own widgets). Also holds `connect_makcu(config)` / `disconnect_makcu(config)` — thin wrappers around `win_utils.makcu_mouse`'s already GUI-free, already lock-safe-off-the-GUI-thread `connect_makcu()`/`disconnect_makcu()`; the wrappers exist only to read `config.makcu_com_port`/`makcu_baud_rate` (in place of `keys_page.py`'s combo boxes, which a web caller doesn't have) and to guard against an empty/unconfigured port, not to add new hardware logic. `resolve_model_path(model_path)` extracts `start_ai_threads()`'s own pre-flight validation (must end `.onnx`, must exist once resolved against `project_root`) into a standalone, pure `os.path` function — no `onnxruntime`/`win32api` import, so unlike the rest of `start_ai_threads()` it's fully unit-testable, and it's shared with any future caller needing the identical check. `start_ai_threads()` itself also had its `config.Running = True` moved from before its three failure-return points to immediately before thread creation — a failed start used to leave `Running` incorrectly `True` with no thread actually running, harmless when the only caller was a GUI combo box listing real files, but a real risk once a web route can pass an unvalidated path. `request_model_change(config, model_path, inference_backend=None)` mirrors `model_page.py`'s own decide logic (`session_utils.needs_trt_build()`, the DirectML-crossing restart condition) with none of its GUI rendering — refusing outright (`"needs_conversion"`/`"needs_restart"`) instead of ever kicking off a 1-5 minute build or an app restart over HTTP. Its DirectML check is deliberately stricter than `model_page.py`'s own (which fires whenever either side of the comparison is `"directml"` at all, with no requirement that they actually differ — harmless there only because Qt's `currentTextChanged` doesn't fire on an unchanged combo value): this function requires an actual crossing, so a same-backend model-only switch while already on DirectML isn't wrongly refused. Evaluates the TensorRT-build question against a `copy.deepcopy(config)` with the *requested* backend applied (same technique `ConfigManager.preview_config_changes()` already uses safely on a live `Config`) rather than the real config, so nothing is written until the whole decision is known to be safe; the write itself happens under `_multi_field_lock`, making this the second multi-field Config command after `set_always_aim`. `model_page.py` itself is untouched — same precedent as MAKCU, whose GUI button still calls `win_utils.makcu_mouse` directly rather than through `app_controller`. Every heavy import (`win_utils`/`onnxruntime`/`.ai_loop`/`.auto_fire`/`.session_utils`) is deferred inside each function body rather than at module top level — same reasoning as `ai_loop_utils.py`'s deferred `win32api` import — so the module itself (and anything that only needs e.g. `set_always_aim`) stays importable on a machine without those packages. `set_web_esp_enabled(config, enabled)` / `restart_web_esp_if_running(config)` are the Web ESP counterpart of `set_always_aim`: writing `config.web_esp_enabled` alone wouldn't actually start/stop the real `esp_server`, so these mirror `visuals_page.py`'s `_onWebEspEnableChanged()`/`_restartWebEspIfRunning()` — the former starts/stops the live server via a deferred `from core import esp_server`, the latter restarts it (a no-op if not running) so an already-running server picks up a changed `web_esp_http_port`/`web_esp_ws_port`. Both return `esp_server.is_running()` so the caller/route always reports real post-call state rather than an assumed one.
- **A module-level `_multi_field_lock`** in `app_controller.py` guards only *multi-field* Config writes (currently just `set_always_aim`'s coupled pair) — ordinary single-field `setattr` elsewhere in this codebase stays unlocked, relying on the GIL for atomicity, unchanged. Deliberately a module-level lock, not a `Config` attribute: `ConfigManager.preview_config_changes()`'s `copy.deepcopy(config_instance)` raises `TypeError: cannot pickle '_thread.lock' object` on a raw `threading.Lock` instance attribute — confirmed directly — so a lock must never live on `Config` itself. A second, separate module-level `_makcu_connect_lock` guards only `connect_makcu()`, serializing two overlapping connect attempts racing on the same port (e.g. the Qt GUI and a web client both clicking connect within the same handshake window) — `disconnect_makcu()` needs no lock, its teardown path is idempotent-safe.
- **`web_control_settings.py`** — a second web-only module, deliberately separate from `app_controller.py`: nothing here is ever called by a Qt slot (unlike every `app_controller.py` function, which is a boundary "a Qt slot and a web route both call into"), so splitting it out keeps that framing honest rather than diluting it with web-only code. Field names/ranges/descriptions in its `_SCHEMA` dict were copied by hand from reading `model_page.py`/`capture_page.py`/`inference_page.py` — not imported at runtime, since those are Qt GUI files and importing one here would drag PyQt6/qfluentwidgets into the web control server, defeating the whole point of keeping this call chain Qt-free. Every field name was cross-checked against `Config._FIELD_MAP`/`__init__` before being added, since a typo'd `setattr()` wouldn't raise — it would just silently create a fresh unpersisted attribute. Two routes cover ~133 plain Config fields across the Model/Capture/Inference/Aim/Keys & HW/Visuals panels instead of one dedicated route per field: `get_tab_settings(config, tab)` reads every field in `_SCHEMA[tab]` in **display units** (e.g. `screenshot_interval` is stored on `Config` in seconds but shown/edited in ms via a `scale: 1000` entry — the same unit the GUI's own slider already uses); `apply_tab_settings(config, tab, updates)` validates a partial `{field: display_value}` dict in two passes (validate everything first, write nothing until the whole body is known-good — same atomicity precedent as `request_model_change()`), returning `{"ok": False, "reason": ..., "field": ...}` for `unknown_field`/`invalid_choice`/`invalid_value`, or `{"ok": True, "applied": {...}}`. A field name may be a dotted/indexed path (`_get_nested()`/`_set_nested()`) for values that don't live directly on `Config` — `"humanization.intensity"` reaches `config.humanization.intensity` (`Config.humanization` is its own `HumanizationConfig` dataclass) and `"AimKeys.0"` reaches `config.AimKeys[0]` (a `list[int]`); a plain (undotted) field behaves exactly as a bare `getattr`/`setattr`, so this is purely additive. `"choice"` fields normally list string choices matched case-insensitively, but a couple (`cam_motion_comp_size`, `makcu_baud_rate`) have **int**-valued choices instead — matched by string comparison then stored back as the matched choice's own type, so an int-valued choice list round-trips as int rather than being coerced into a string. Coupled writes the GUI does as one action (picking an NDI source also sets `ndi_force_reconnect`; picking a game profile also sets `hud_roi_coords` from `game.json`; switching Move Method to `"makcu"` also sets `mouse_click_method`; turning off PID Unsafe Mode also clamps `pid_kp_x`/`pid_kp_y` to ≤0.5) are just multiple keys in one POST body from the client, which already has every value in hand — no server-side "decide" logic needed, unlike `set_always_aim`/`request_model_change`. The module also holds thin single-purpose helpers, each mirroring one non-setting GUI feature: `get_model_info()` (live model inspector via `model_detect.inspect_model()`, preferring a cached `trt_cache/*.engine` over the raw `.onnx` when TensorRT is active, same as `_updateModelInfo()`), `get_model_notes()`/`save_model_notes()` (same `model_info.json` and default template as `_ModelNotesCard`), `open_model_folder()` (`os.startfile(Model/)` — **opens Explorer on the machine running Axiom, not the remote browser**, same as the Qt button), `get_game_profiles()`/`get_hud_models()` (`game.json` / `Model_Hud/*.onnx` listings), `probe_uvc()`/`get_ndi_sources()` (deferred-import wrappers around `core.screen_capture`'s existing device/source enumeration, combining `_UvcProbeWorker`'s three calls into one response), `get_local_ips()` (reimplements `capture_page.py`'s module-level `_get_local_ips()`, pure `socket` logic), `list_vk_options()` (named VK-code options for the Keys & HW panel's hotkey selects, sourced directly from `win_utils.vk_codes.VK_CODE_MAP` — a plain pure-Python table, not a GUI file, so it's reused rather than copied, unlike `keys_page.py`'s own separate/parallel `VK_CODE_TRANSLATION_MAP`), `get_serial_ports()` (`serial.tools.list_ports.comports()`, for the MAKCU COM Port select), and `reset_humanization()` (replaces `config.humanization` with a fresh `HumanizationConfig()`, reusing the real dataclass's own defaults so it can never drift from `core/humanization.py`). `open_web_esp_in_browser()` (`webbrowser.open()` on the running Web ESP overlay's `esp_server.connect_url()` — **opens on the host machine running Axiom, not the remote client**, same caveat as `open_model_folder()`; returns `False` if the server isn't currently running) is the Visuals panel's remaining non-setting action, alongside a `get_tab_settings("visuals")` extras block (`web_esp_enabled`/`web_esp_running`/`web_esp_url`, read-only — `web_esp_enabled`'s canonical write is the `set_web_esp_enabled()` action route above, not a plain schema field, for the same reason `always_aim` isn't one). **Config presets** — `_config_manager()` returns a fresh `core.config_manager.ConfigManager()` per call rather than threading the Qt app's real instance through `web_control_server.start()`: `ConfigManager` holds no state beyond a directory path, so a throwaway instance pointed at the same `"config"` directory behaves identically (pure file I/O) — a simpler design than plumbing a new server-start parameter through `main.py`. `list_config_presets()`, `save_config_preset()`, `preview_config_preset()` (dry-run diff, mirroring `ConfigManager.preview_config_changes()`'s own GUI usage), `load_config_preset()`, `delete_config_preset()`, `rename_config_preset()`, and `open_configs_folder()` (`os.startfile(configs_dir)`, same host-only caveat) are thin wrappers with no logic of their own beyond the matching `ConfigManager` method. `export_config_preset_content(name)` / `import_config_preset_content(content)` are the one genuinely new pair: `ConfigManager.export_config(name, path)`/`import_config(path)` both operate on a **host-side file path**, which a browser fundamentally cannot supply (a file input only ever exposes a picked file's *content*, never its path on the picking machine) — so these reimplement the equivalent operations directly against JSON content instead of a path: export reads and returns the preset file's raw text (for the client to offer as a `Blob` download) while import re-derives the same name-extraction/uniqueness-suffix (`_1`, `_2`, ...) logic `ConfigManager.import_config()` already has, reusing its `_sanitize_config_name()` so an untrusted `name` field from the uploaded JSON still can't escape `configs_dir`.
- **`web_control_server.py`** — FastAPI + uvicorn (not the stdlib approach `esp_server.py` uses): a control-plane server needs real request validation and inbound-frame handling that hand-rolled RFC 6455 (push-only, never parses a client frame) doesn't provide. Runs uvicorn's ASGI server inside this process on its own background thread (`_ThreadServer`, a `uvicorn.Server` subclass with `install_signal_handlers()` overridden to a no-op — installing signal handlers only works on the main thread). Every route requires a shared token via the `X-Axiom-Token` header (`config.web_control_token`, auto-generated with `secrets.token_urlsafe(24)` the first time the feature is enabled and empty) checked by a FastAPI dependency — unlike Web ESP's explicitly-documented no-auth posture, this server can mutate state, so it can't reuse that stance. `GET /api/status` returns a smaller, control-oriented field set (aim state, model, FPS, MAKCU connection + port, `running`) — not a reuse of `esp_server._build_snapshot()`. Control actions so far: `POST /api/control/always_aim` (calling `app_controller.set_always_aim()`), `POST /api/control/makcu_connect` / `POST /api/control/makcu_disconnect` (calling the `app_controller` MAKCU wrappers), and `POST /api/control/ai_start` / `POST /api/control/ai_stop` (calling `app_controller.start_ai_threads()`/`stop_ai_threads()` with the same overlay/auto-fire queues `main.py` already created — passed into `web_control_server.start()` via three new optional kwargs so a web-started AI thread's output reaches the same overlay/auto-fire consumers the GUI-started one would). `ai_start` always uses `config.model_path` — no caller-supplied path — that's the model-switching route's job. `POST /api/control/model` (`model_path`, optional `inference_backend`) is that route, calling `app_controller.request_model_change()`; bundled as one route rather than split, since the decide logic inherently needs both pieces together — a model-only change can still trigger `needs_conversion` under the current backend, and a backend-only change can still trigger `needs_restart`/`needs_conversion` under the current model. `GET /api/models` (token-gated, alongside `GET /api/status`) returns `{"models": [...]}`, delegating to `app_controller.list_models()` — sorted `.onnx` basenames under `Model/`, reimplementing `model_page.py`'s own glob+sort expression rather than importing from it (`model_page.py` stays untouched, same precedent as MAKCU/`request_model_change` above); no TensorRT cache-badge info like `model_page.py`'s own ✓/⬇ icons, out of scope for this route. The web client's model field is a `<select>` populated from this route once on page load, not a free-text path input. `GET`/`POST /api/settings/{tab}` (`tab: Literal["model","capture","inference","aim","keys","visuals"]`, an unknown tab 422s automatically via FastAPI's own literal validation) delegate straight to `web_control_settings.get_tab_settings()`/`apply_tab_settings()`; `GET /api/model_info`, `GET`/`POST /api/model_notes`, `POST /api/control/open_model_folder`, `GET /api/game_profiles`, `GET /api/hud_models`, `GET /api/uvc_probe`, `GET /api/ndi_sources`, `GET /api/vk_options`, `GET /api/serial_ports`, and `POST /api/control/humanization_reset` each delegate to that module's matching single-purpose helper. `POST /api/control/web_esp_enabled` (body: `{"enabled": bool}`, calling `app_controller.set_web_esp_enabled()`), `POST /api/control/web_esp_restart` (calling `app_controller.restart_web_esp_if_running()`), and `POST /api/control/web_esp_open` (calling `web_control_settings.open_web_esp_in_browser()`) are the Visuals panel's Web ESP action routes — deliberately actions rather than generic-schema fields, since flipping `web_esp_enabled` must actually start/stop the live server. Config preset management gets its own small route group, all delegating to the matching `web_control_settings.py` function: `GET /api/configs` (list), `POST /api/configs/save` (body: `{"name": str}`), `GET /api/configs/preview?name=...` (dry-run diff), `POST /api/configs/load`, `POST /api/configs/delete`, `POST /api/configs/rename` (body: `{"old_name": str, "new_name": str}`), `GET /api/configs/export?name=...` (returns raw JSON content for the client to download), `POST /api/configs/import` (body: `{"content": str}` — the uploaded file's raw text, not a path), and `POST /api/control/open_configs_folder`. A failed MAKCU connect (bad/unset port, failed handshake), a failed `ai_start` (bad/missing model path, provider/session load failure), or a refused model/backend change (bad path, bad backend, needs a restart, needs a build) is a normal runtime outcome, not a request error, so all of these come back as HTTP 200 with `{"ok": false, "reason": ...}` rather than an error status — HTTP error codes stay reserved for the auth/validation layer (401 bad token, 422 malformed body). `fastapi`/`uvicorn` are vendored straight into `src/python/dependencies/` — the same tracked-in-git directory `main.py` already puts on `sys.path` unconditionally at startup, alongside `qfluentwidgets`/`vgamepad`/`pywin32`/`directshow_capture.dll` — rather than installed on demand via a separate script, so no first-run install step is needed. Every import of them in `web_control_server.py` is still deferred inside `start()` rather than at module top level, so the module stays importable (and `start()` fails soft — logs and returns `False`) on a checkout that predates them being vendored in, the same way `main.py` already wraps `esp_server.start()` in a try/except.
- **`src/web_control_client/`** — sibling to `src/web_overlay/`, same plain HTML/JS/CSS style (no framework, no build step). A tabbed layout mirrors the real Qt app's `NavigationInterface` order (Model → Capture → Inference → Aim → Trigger → Keys & HW → Visuals → Configs → Convert → Other — see the GUI section's nav table): a top bar (`.topbar`) holds the wordmark, one `.tab` button per page (`data-tab="model"` etc., toggled via `app.js`'s `activateTab()`), and the connection-status badge; a persistent left `.sidebar` holds the Access Token field and a trimmed cross-cutting Status grid (running/aim-toggle/firing/mouse-method/MAKCU/MAKCU-port/capture-FPS/inference-FPS — facts relevant no matter which tab is open); the `.main` area holds one `.panel` per tab, only the active one visible (plain CSS `display: none`/`flex`, so `poll()`'s `setInterval` and both toggles' echo-suppression flags keep working on hidden-panel elements exactly as on visible ones — exercising a hidden `<input>`/`<select>` via `change`/`.checked`/`.value` is ordinary DOM behavior). Eight panels are functional today — **Model** (model/backend switch, Model Info, Model Notes, Open Model Folder, Model HUD Settings), **Capture** (Capture Method + conditional UVC/NDI/UDP groups), **Inference** (AI Start/Stop, plus FOV & Detect Range / General Parameters / Inference Performance), **Aim** (General params, Aim Speed (PID) X/Y, Y-Axis Recoil Suppression, Humanization incl. Reset to Defaults, Target Priority, Target Tracking, Target Area Settings — Arduino and Xbox 360 Controller groups skipped, same "too complex to replicate remotely" reasoning as Capture's Preview/Active Weapon groups: both involve live hardware connect/calibration actions, not just Config fields), **Keys & HW** (Always Aim + MAKCU Connect toggles; Aim Keys 1-3/Toggle Key/Auto-Fire Keys 1-2 as named-option `<select>`s instead of the Qt app's live "press a key" capture — see below; MAKCU Connection group (COM Port + Baud Rate); MAKCU Keys group (Inference/Auto Aim Key/Aim Mode/Disengage Delay)), **Visuals** (Display/Status Panel/Crosshair/Appearance groups, plus a Web ESP group whose Enable toggle and Open button are dedicated actions — not generic-schema fields — so the live `esp_server` actually starts/stops/reopens rather than just writing a Config flag), and **Configs** (a `<select size="8">` listbox of presets — the native HTML equivalent of the Qt `ListWidget` — with Create/Load/Save/Rename/Delete/Export/Import/Open Folder buttons; `window.prompt()`/`window.confirm()` stand in for `QInputDialog`/`QMessageBox`; Load previews the diff before applying, mirroring the Qt page's own pre-load confirmation; Export/Import are content-based — a `Blob` download and a hidden `<input type="file">` + `FileReader` upload — since a browser can't supply a host-side file path the way the Qt dialogs do) — the remaining two (Trigger, Convert) are still "Coming soon" placeholders with no backend to call yet.

**Hotkey rebinding** (Keys & HW panel) — the Qt GUI's `KeyBindButton` live-captures a keypress via a global `win32api.GetAsyncKeyState()` poll (works even while the app is unfocused); that mechanism has no safe remote-browser equivalent — there's no way to poll OS-level key state from a web page, and browser key/mouse-button codes (`KeyboardEvent.code`, `MouseEvent.button`) don't correspond to this app's own VK-code scheme at all, so a naively-translated live-captured browser event could silently bind the wrong input. Each hotkey field is instead a `<select>` populated once from `GET /api/vk_options` (`app.js`'s `ensureVkOptionsLoaded()`, matching every `.vk-select` element) — sacrificing "press to bind" UX for correctness. `AimKeys[0]` (`AimKeys.0`) is written by two different UI elements depending on mode — the plain "Aim Key 1" `<select>` (non-MAKCU) and the MAKCU "Inference" `<select>` (a small fixed set of mouse buttons, MAKCU can't relay a capturable key event) — both are ordinary generic fields with no coupling, they just happen to share one underlying Config field the same way the GUI itself does. Polls `GET /api/status` every second regardless of which tab is active (the sidebar is always visible); fetches `GET /api/models` once on page load (`loadModelList()`) to populate the Model panel's `<select>`, not every poll tick — `applyModelSelection()` reconciles that fetch against the first status poll's `model` field regardless of which of the two arrives first. The Always-Aim toggle is wired to `POST /api/control/always_aim` and the MAKCU Connect toggle to the two MAKCU routes — the latter's `change` handler reverts the toggle both on a transport-level failure (`!res.ok`) and on a normal `200 {"ok": false}` body, unlike the always_aim toggle (which can't fail that second way). The backend `<select>` folds a `"cuda"` status value onto its `"TensorRT"` option for display (mirroring `model_page.py`'s own reverse-map fold) but never writes `"cuda"` back in a POST body. The access token is entered once and kept in that page's own `localStorage` — never sent anywhere except the `X-Axiom-Token` header on each request.
  - **Generic settings fields** — every plain-Config-field element in the Model/Capture/Inference panels carries `data-key`/`data-type` attributes (e.g. `data-key="fov_size" data-type="number"`); `app.js`'s `fieldElsFor(tab)` (`querySelectorAll('#panel-' + tab + ' [data-key]')`) drives both directions generically: `loadTabSettings(tab)` (called on every tab activation, plus once at startup for the default-active Model tab, so a change made from the Qt GUI while the tab was backgrounded is picked up on return) applies each field's fetched value via `applyGenericValue()`, skipping any element that currently has focus so an in-progress edit is never clobbered; `wireGenericFields(tab)` attaches a `change` listener that POSTs just `{[key]: value}` via `pushTabSetting()` — never a full-form resync, so unrelated fields can't be stomped by a stale value. An element marked `data-custom="1"` (the Game Profile `<select>`) is still read generically but excluded from `wireGenericFields()`'s generic write path, since its own change needs a coupled two-field POST instead (see `web_control_settings.py`'s coupled-write note above). A handful of fields need genuinely custom handling beyond generic get/set: the UVC Resolution `<select>`'s value is a derived `"WxH"` string with no single matching Config field, so its `change` listener splits it and POSTs `uvc_width`/`uvc_height` as two separate calls; the NDI Source `<select>`'s `change` listener POSTs `ndi_source_name` + `ndi_force_reconnect` together, mirroring `_onNdiSourceChanged()`. Visibility mirrors the GUI's own conditional cards: `updateCaptureVisibility()` shows only the UVC/NDI/UDP group matching the current `screenshot_method` (same condition as `_updateCaptureControlsVisibility()`), `updateUvcSubVisibility()` nests the DirectShow-V2/FFmpeg/FFmpeg-path cards under `uvc_capture_method`/`uvc_dshow_backend`/`uvc_ffmpeg_enabled` (mirrors `_updateDshowControlsVisibility()`), `updateFovReduceVisibility()` shows the FOV Min Size/Duration cards only while `fov_reduce_on_target_enabled` is on (mirrors `_updateFovReduceVisibility()`), `updateHumanizationVisibility()` gates each of the 5 feature sub-groups on (master enabled AND that feature's own toggle) — reading straight from the live DOM checkboxes rather than a cached settings object, so it's correct whether called right after a `change` event or after a fresh `loadTabSettings("aim")` — and `updateKeysVisibility()` toggles the Aim Keys/Auto-Fire groups vs. the MAKCU Connection/MAKCU Keys groups on `mouse_move_method === "makcu"`, the MAKCU Inference card on `keep_detecting`, and the MAKCU Auto Aim Key/Aim Mode/Disengage Delay cards on `always_aim` (the last of these is also kept live-synced from every `GET /api/status` poll, mirroring `_updateMakcuAimStatus()`'s own continuous re-check, so it updates even while the Keys & HW tab isn't the active one). Two of the three read-only extras `updateKeysVisibility()` needs (`mouse_move_method`, `keep_detecting`) ride along on `GET /api/settings/keys`'s response even though their canonical write lives on a different tab's schema (`aim`/`inference` respectively) — same "cheap to compute alongside what's already being read" reasoning as Capture's extras. Read-only "actual negotiated" readouts (`uvc_actual_*`, `ndi_width`/`ndi_height`/`ndi_source_nominal_fps`) and the UDP System-IP/Bind-IP-options list ride along as extra, non-schema keys on `GET /api/settings/capture`'s response (`applyCaptureExtras()`) rather than their own routes, since they're cheap to compute alongside the fields that are already being read. The Model panel's search box (`modelSearchInput`) is a pure client-side filter over the already-fetched model list — no backend call — mirroring `modelSearchEdit`'s "never hide the currently active model" behavior. Numeric GUI sliders (FOV Size, Detect Interval, Min Confidence, etc.) are plain `<input type="number">` fields here rather than replicated slider widgets — same value, same min/max/step enforcement (both client-side via the input's own attributes and server-side via `_SCHEMA`), simpler markup. The Visuals panel's Web ESP group is the one place a generic-schema tab still needs custom JS: `web_esp_enabled` has no `data-key` (see `_SCHEMA["visuals"]`'s comment), so it gets its own `change` listener — POSTing to `/api/control/web_esp_enabled` with the same disable-during-request + revert-on-failure pattern the MAKCU toggle uses — and `applyVisualsExtras()` (called from `loadTabSettings("visuals")`) syncs that toggle plus the read-only connect-URL line from the tab's `web_esp_enabled`/`web_esp_running`/`web_esp_url` extras. A `web_esp_http_port`/`web_esp_ws_port` change still goes through the generic field path (it does have a `data-key`) but additionally calls `scheduleWebEspRestart()` — a 600ms debounce (clearing any prior pending timer) before `POST /api/control/web_esp_restart`, mirroring `visuals_page.py`'s own `_webEspRestartTimer` so rapid edits collapse into one restart instead of one per keystroke.

  **Configs panel** — has no `data-key` fields at all (a config preset isn't a Config attribute, it's a named file), so it's wired entirely by hand rather than through the generic mechanism: `refreshConfigsList()` (called on every Configs tab activation, plus by its own Refresh button) populates the `<select size="8">` listbox from `GET /api/configs`; Create/Save/Rename/Delete each collect their argument via `window.prompt()`/`window.confirm()` (the direct browser-native substitute for `QInputDialog.getText()`/`QMessageBox.question()`, since this is a plain no-framework client) before POSTing. Load calls `GET /api/configs/preview` first and only confirms via `window.confirm()` when the diff is non-empty — an empty diff or a failed preview both fall through to loading directly, mirroring `configs_page.py`'s own "skip the prompt when nothing would change" behavior — and on success re-runs `loadTabSettings()` for every generic-schema tab, since a loaded preset can touch fields anywhere. Export builds a `Blob` from `GET /api/configs/export`'s raw JSON content and clicks a temporary `<a download>` link (this is the app's own hosted static page, not a sandboxed Artifact, so a download link isn't blocked); Import reverses that — a hidden `<input type="file">` triggered by a visible button, whose `change` handler reads the picked file's content via `FileReader` and POSTs it to `/api/configs/import` — since a browser can only ever hand over a file's *content*, never a host-side path the way the Qt Export/Import dialogs use.
- **GUI wiring** — a "Remote Control" `SettingCardGroup` on `other_page.py` (Enable switch, port field with the same 600ms-debounced restart-on-change pattern as Web ESP's `visuals_page.py`, a token display with Copy/Regenerate buttons, an "Open in browser" button). Separately, `other_page.py`'s own MAKCU Hardware status (`_makcuHwTimer`, 3s) already re-queries `is_makcu_connected()` live on every tick, so it picks up a web-triggered connect/disconnect for free; `keys_page.py`'s Connect/Disconnect button did not — its status refresh only ran from four page-local event sites — so `_updateMakcuAimStatus()` (its one recurring 50ms timer) now also compares against `self._isMakcuConnected` and refreshes the button/label on a mismatch, closing that gap without adding new polling infrastructure.
- **Rollout was intentionally incremental** — `always_aim` was the first control action because it already had a real GUI checkbox with a well-understood side effect, exercising the full stack (auth → route → `app_controller` → `Config` → Qt reflection) at minimum risk. MAKCU connect/disconnect followed as the next-smallest slice, since `win_utils.makcu_mouse` was already GUI-free with nothing to extract. AI start/stop followed that: `web_control_server.start()` gained three optional queue kwargs (`overlay_boxes_queue`/`overlay_confidences_queue`/`auto_fire_boxes_queue`), which `main.py` passes the same objects `PyQtOverlay`/`auto_fire_loop` already read — never independently-created ones — so `POST /api/control/ai_start`/`ai_stop` call the exact already-existing `start_ai_threads()`/`stop_ai_threads()`. `pause_ai_inference()`/`resume_ai_inference()` are deliberately left unwired — they exist for a local GPU-driver-upgrade window per their own docstrings, have no GUI caller anywhere in `src/` today, and don't fit a remote on/off control the way `Running` already does. Model/provider switching was the last of the four slices named when this rollout started, closing it out with `request_model_change()`/`POST /api/control/model` as described above. A fifth slice followed: the client's model field became a real `<select>` synced against the Qt app's actual `Model/` contents (`app_controller.list_models()` / `GET /api/models`) instead of a free-text path the operator had to get right by hand, and the whole page was restructured into the tabbed Model/Capture/Inference/.../Other layout described above — laying out where every future per-tab control action will eventually live, even though most tabs were still placeholders. A sixth slice filled in three of those placeholders for real: Model (Model Info, Model Notes, Open Model Folder, Model HUD Settings), Capture (Capture Method + conditional UVC/NDI/UDP groups), and Inference (FOV & Detect Range, General Parameters, Inference Performance) — introducing `web_control_settings.py`'s generic tab-settings mechanism specifically so this could cover ~39 fields without 39 near-identical dedicated routes. Deliberately out of scope for that slice, as genuinely harder to replicate remotely than a plain settings form: the Capture page's Preview group (live capture preview frames) and its "Active Weapon" OCR/HUD group, and the Inference page's Weapon Detection Method/Threshold controls — all three involve live image/detection state, not just Config fields. A seventh slice filled in two more: Aim (General params, Aim Speed (PID), Y-Axis Recoil Suppression, Humanization incl. Reset to Defaults, Target Priority, Target Tracking, Target Area Settings) and Keys & HW (the remaining hotkey slots, MAKCU Connection, MAKCU Keys) — extending `_SCHEMA` with dotted/list-index field paths (`_get_nested()`/`_set_nested()`, for `config.humanization.*` and `config.AimKeys[N]`) and int-valued `"choice"` fields, both purely additive generalizations of the existing mechanism, needing no new architecture. Deliberately out of scope for that slice, same "too complex/hardware-bound to replicate remotely" reasoning as before: Aim's Arduino and Xbox 360 Controller groups (COM port enumeration, board-spoofing, physical test-move, ViGEmBus connect — all live hardware actions), and the Qt GUI's live hotkey-capture mechanism itself (replaced with a named-VK-constant `<select>`, sourced from `win_utils.vk_codes` — see the client bullet above for why). An eighth slice filled in the last two panels with real backends: Visuals (Display, Status Panel, Crosshair, and Appearance groups through the generic schema, plus a Web ESP group whose Enable toggle and Open button are new dedicated `app_controller.py`/`web_control_settings.py` actions rather than schema fields — `set_web_esp_enabled()`/`restart_web_esp_if_running()` mirror `visuals_page.py`'s own live start/stop/restart of the real `esp_server`, the same "a bare Config write wouldn't actually do anything" reasoning as `always_aim`) and Configs (full preset CRUD through a new `ConfigManager`-backed route group, plus content-based Export/Import designed specifically around a browser's file-access model — see the `web_control_settings.py` bullet's "Config presets" section). Further growth of the Web Control API (the two remaining placeholder tabs — Trigger and Convert — or the Arduino/Xbox groups skipped above) remains incremental, one slice at a time, consistent with this feature's "control main functions, not every widget" framing — not a fixed roadmap item.

### In-Game Overlay & Status Panel (`src/gui/`)
The actual on-device renderer referenced above — plain PyQt widgets, not part of `fluent_app/`:
- **`overlay.py`** — transparent, click-through `QWidget` drawing FOV/detect-range circles, detection boxes, confidence text, tracer lines, and the aim-point X marker directly over the game window. Colors go through the `OverlayColors` static-method wrapper (`get_fov_color()`, `get_box_color()`, `get_tracer_color()`, `get_aim_marker_color()`, etc.), each backed by a `ThemeColors.OVERLAY_*` entry — never hardcode a color here directly, add a new `ThemeColors` entry and getter instead. `box_color_theme` can override the detect-box color with a named preset instead of the theme default.
- **`status_panel.py`** — the on-screen status HUD (aim state, FPS, model name, MAKCU/Xbox connection). Supports Windows Acrylic blur via the legacy `SetWindowCompositionAttribute` API; re-reads language strings on its own refresh timer rather than needing a `retranslateUi()` call.

### Other Standalone Dev Tools (`src/core/`)
Not wired into the GUI or main loop; run directly for diagnosis/tuning.
- **`obs_inspect_filters.py`** — read-only OBS WebSocket inspector (`python src/core/obs_inspect_filters.py --source "Your Source Name"`). Connects to OBS's built-in WebSocket server (port 4455) and dumps every filter's full settings dict for a given source — used to check whether `udp_stream_filter` (or any other filter) exposes its own resolution/scale/quality setting without hunting through the OBS UI by hand. Never modifies OBS state. `--list-sources` enumerates sources if you don't know the exact name.
- **`bench_udp.py`** / **`_bench_udp_sender.py`** — standalone UDP receive-pipeline benchmark for the `UdpCapture` backend (`python src/core/bench_udp.py decode-bench|loopback-bench ...`). Isolates whether a low observed FPS is real packet loss (`live` mode, watch `dropped_fps`), JPEG decode CPU cost (`decode-bench`, measures the `cv2.imdecode` ceiling with no networking), or raw UDP-assembly throughput (`loopback-bench`, spins up a real `UdpJpegReceiver` on `127.0.0.1` and fires synthetic frames at it from `_bench_udp_sender.py` — a genuinely separate OS process, not a thread, so it doesn't share a GIL with the receiver, matching the real deployment where the OBS sender is an independent process).

### Mouse/Device Backends (`src/win_utils/`)
All backends expose `send_mouse_move_<method>(dx, dy)` and `send_mouse_click_<method>()`. Selected at runtime from `config.mouse_move_method`. Methods:
- `sendinput` / `mouse_event` — Windows `SendInput` / `mouse_event` API
- `makcu` — MAKCU USB HID device at 4 Mbaud (`_OPERATING_BAUD`), ASCII protocol; see `docs/MAKCU_Native_API.md` for the wire protocol reference. `move()` and `click()` must never hold `self._lock` across a `time.sleep()` — lock must be released before any sleep. Scroll-wheel input excluded from aim-button detection (masked to bits 0-4, `_BTN_BITS`). Self-healing: a background reconnect-watchdog thread retries the connection automatically on USB glitches. MAKCU click state is reported to `status_panel.py` via a dedicated callback. `makcu_mouse_binary.py` (the V2 binary-protocol variant) is unused in production today — kept in place for a future MAKCU firmware version; don't wire it in without first fixing the lock-across-sleep violation in its `connect()`/`_send_cmd()`.
- `arduino` — Arduino Leonardo HID (`arduino_mouse.py`, `arduino_spoofer.py`)
- `ddxoft` — ddxoft driver (`ddxoft_mouse.py`)
- `xbox` — Virtual Xbox 360 controller via ViGEmBus (`xbox_controller.py`, `gamepad_input.py`)

### GUI (`src/gui/fluent_app/`)
PyQt6 + PyQt6-Fluent-Widgets. `window.py` hosts a `NavigationInterface` (min 40 px, expand 150 px) with 10 pages:

| Page class | Object name | Purpose |
|---|---|---|
| `VisualsPage` | `displayInterface` | Overlay display settings |
| `ModelPage` | `modelInterface` | Model selection & notes |
| `CapturePage` | `captureInterface` | Screen capture backend |
| `InferencePage` | `inferenceInterface` | Model inference settings |
| `AimPage` | `aimInterface` | Aiming algorithm parameters, Anti-Recoil |
| `TriggerPage` | `triggerInterface` | Triggerbot / auto-fire |
| `KeysPage` | `keysInterface` | Hotkey bindings + MAKCU device connection (nav label: "Keys & HW") |
| `ConfigsPage` | `configInterface` | Config presets |
| `ConvertPage` | `convertInterface` | ONNX → TensorRT conversion |
| `OtherPage` | `otherInterface` | Mouse method, performance, misc |

Each page extends `BasePage` and calls `setConfig(config)` to bind to the live `Config` object. Shared widget primitives live in `fluent_app/components/slider_spin_card.py` (`SliderLabelCard`, `SliderDoubleSpinCard`).

`window.py` also mounts a **`CapturePreviewPanel`** (`fluent_app/components/capture_preview.py`) as a collapsible side panel with a `◀` arrow toggle; the panel polls `screen_capture.get_preview_frame()` and displays a live feed with FPS counter and pop-out support.

**Keys page hotkey conflict warning** — `keyConflictCard`, a `SettingCard` banner at the top of `keys_page.py`, hidden unless `_checkKeyConflicts()` finds a genuinely confusing hotkey setup. It deliberately does **not** flag every pair of slots sharing a VK: `_collectKeySlotGroups()` groups slots by *purpose* (the 3 Aim Keys together, the 2 Auto-Fire keys together; in MAKCU mode, Inference and Auto Aim Key are each their own single-item group) and only two things get flagged — a redundant duplicate *within* one group (e.g. Aim Key 1 == Aim Key 2, wastes a slot, almost certainly accidental), and the edge-triggered toggle key (`aim_toggle_key`, flips `AimToggle` on press — see `key_listener.py`) landing on the same VK as any hold-based key (pressing it then also reads as "held" for whatever else has it, and can flip the toggle back off mid-hold). Binding an Aim Key and an Auto-Fire key — or, in MAKCU mode, the Inference and Auto Aim Key buttons — to the *same* button is deliberately never flagged: that's a common, often-intentional "hold one button to do both" setup, and it's this app's own shipped default (`AimKeys[1]` and `auto_fire_key` are both Mouse X2 out of the box), so cross-purpose overlap being flagged would make every fresh install show a false-positive warning. Slot reachability mirrors `_updateMakcuVisibility()`/`_refreshMakcuVisibility()` exactly (a card hidden by mode/`always_aim`/`keep_detecting` is excluded from the scan), and the scan re-runs after every rebind, mode switch, and `retranslateUi()`.

**Theming** (`fluent_app/theme_colors.py`) — a single `ThemeColors` class of `ColorPair`/`ColorPairWithAlpha` descriptors (`.get()` returns a CSS color string, `.qcolor()` a `QColor`), each with light/dark defaults and an optional override from a user `theme_colors.json`. `isDarkTheme()` (qfluentwidgets) picks which side of the pair applies. Any `setStyleSheet(...)` call with a literal hex/rgb color instead of a `ThemeColors.*.get()` reference is a theming bug — it won't adapt to a light/dark toggle. (`theme_manager.py`, a separate unused QSS-generator module, was deleted as dead code — it was never invoked anywhere.)

**First-run flow**: `fluent_app/setup_wizard.py` (Welcome → Language → Theme → Acrylic → Performance → Done, gated on `state.json`'s `first_run_complete`) followed by `gui/disclaimer_dialog.py` (gated on `disclaimer_agreed`; body text loaded from a single English-only `Disclaimer.md`, deliberately not localized — translating just the button chrome around a permanently-English legal document would be a confusing half-measure). The wizard tracks its own in-progress theme preview (`self._isDark`) separately from the app's real theme — `setTheme()` isn't actually called until the wizard closes (`applyChosenTheme()`, invoked from `main.py`), so any wizard-page widget that needs to look right in both previews must read the wizard's own state, not `isDarkTheme()`.

**i18n**: `t(key, default)` (`fluent_app/language_manager.py`) looks up `key` in the active language's JSON (`src/core/language_data/*.json`, 10 languages) and falls back to `default` (always English) if the key is missing — a missing key never crashes or shows blank, just shows English regardless of the selected language. Widgets built with a `t()`-wrapped title/description at construction must have the *same* call repeated inside that page's `retranslateUi()` (called from `window.py`'s `_refreshUI()` on every language switch) or the text goes stale after the first switch — this has been a recurring bug source; when adding a new card/label, grep an existing correct one in the same file for the exact `titleLabel`/`contentLabel` API before wiring a new one in. Translation completeness varies — English has the most keys (~290); other languages are missing anywhere from ~20 to ~75 of them (pre-existing, not something a single pass has closed).

### ONNX / TensorRT
Models go in `Model/` (`Model_Hud/` for the secondary weapon-detector model). The preprocess fast-path is automatic and backend-agnostic, driven purely by the captured frame's own shape (not a config flag): an exact `model_input_size × model_input_size` frame skips resize/letterbox entirely (`preprocess_image()`, `inference.py`); a square-but-different-size frame gets a plain nearest-neighbor resize with no padding (`fast_resize`, derived in `ai_loop.py` from `frame.shape[0] == frame.shape[1]`); only a non-square frame gets a real letterbox with padding. TensorRT engines are built by `src/core/convert_to_engine.py` and cached in `trt_cache/` at the project root (`session_utils.py`'s `_TRT_CACHE_DIR`) — **not** `%LOCALAPPDATA%\AxiomAI\`, which is only where the TensorRT *pip package itself* gets installed by `Install TensorRT.bat`, a separate concern. `cuda_io_binding_enabled` enables zero-copy CUDA inference (CUDA provider only).

Both `model_page.py` and `convert_page.py` pair their model `ComboBox` with a `SearchLineEdit` (`modelSearchEdit`) that live-filters the combo by case-insensitive filename substring as the user types — a plain filter, not an editable combo, so there's no way to type-and-Enter a phantom entry that doesn't correspond to a real `.onnx` file. Each page keeps its own master file list (`self._all_model_files`, refreshed by `_refreshModelList()`) and re-derives the combo's visible subset from it in `_onModelSearchChanged()`, which always keeps whatever's currently selected in the list even if the search text doesn't match it — so narrowing the search can never make the combo appear to show the wrong "currently active" model.

**A model with no cached `.engine` yet is a 1-5 minute *synchronous* build** the first time `onnxruntime.InferenceSession(...)` is created with the TensorRT provider — never let that call happen inline on `ai_loop.py`'s hot-swap path (`_try_hot_swap_model()`, called once per frame on the main inference thread), or the whole aim loop freezes with no progress feedback for the duration. Two layers guard against this:
- **GUI (`model_page.py`)** — before ever writing a model/backend combination to `config` that would make TensorRT the effective provider, `_redirectToConvertIfNeeded()` calls `session_utils.needs_trt_build(config, model_path)`; if true, it switches the window to the Convert tab (`FluentWindow.switchTo()`) and calls `ConvertPage.startConversionFor(model_path)` — the same background-thread + progress-bar build the Convert tab's own button uses — instead of ever setting `config.model_path`/`config.inference_backend` to that combination. `ConvertPage._onConvertFinished()` applies `config.model_path` to the just-built model on success and calls `window._refreshAllPages()`, so the app picks it up automatically (now a near-instant cache hit) without the user re-selecting it. This is wired into both the model combo (`_onModelChanged`) and the backend combo (`_onInferenceBackendChanged`, for the non-restart transitions — DirectML↔other still goes through the existing app-restart flow, which is a separate first-load path this doesn't cover).
- **`ai_loop.py`'s `_try_hot_swap_model()`** — a safety net for anything that bypasses the GUI (a loaded preset, a hand-edited `config.json`): `session_utils.needs_trt_build()` is checked before the `InferenceSession(...)` call; if a build would be needed, the swap is skipped (current model kept running, no freeze) and a warning is logged instead.

`session_utils.find_trt_engine_cache(model_path)` is the shared existence check (mirrors `build_provider_list()`'s `trt_engine_cache_prefix` = model filename stem convention); `effective_first_provider(config)` answers "is TensorRT actually what this config would try first" without duplicating `build_provider_list()`'s auto/backend-selection logic; `needs_trt_build()` combines both.

`model_page.py`'s model combo also surfaces cache status per-entry, before the user even opens the dropdown: `_applyModelBadges()` icons each item ✓ (`FluentIcon.ACCEPT`, cached) or ⬇ (`FluentIcon.CLOUD_DOWNLOAD`, would trigger the 1-5 min build above) via `setItemIcon()` — this only changes the item's icon, never its `.text`, so it can't interfere with the plain-filename string matching the rest of the page relies on (`_onModelChanged`'s `os.path.join("Model", text)`, the search filter's basename comparisons, etc.). `_isTensorRtActive()` (reuses `effective_first_provider()`) hides the badges entirely when TensorRT isn't the active backend, since cache status is meaningless noise under any other provider — a `modelCard` subtitle legend explaining the icons is shown/hidden the same way. Cache status itself (`_model_engine_cached`, one `find_trt_engine_cache()` call per model) is recomputed by `_refreshModelList()`; badge/legend *visibility* is also re-applied on every inference-backend change (`_onInferenceBackendChanged`) and by `_onModelSearchChanged()`'s combo rebuild, without re-scanning disk.

## Key Config Fields to Know

| Field | JSON path | Purpose |
|---|---|---|
| `screenshot_method` | `capture.screenshot_method` | `'mss'` / `'dxcam'` / `'uvc'` / `'ndi'` / `'udp'` |
| `udp_bind_ip` / `udp_bind_port` | `capture.udp.bind_ip` / `.bind_port` | Listen address for the OBS `udp_stream_filter` MJPEG stream |
| `mouse_move_method` | `hardware.mouse_move_method` | `'sendinput'` / `'mouse_event'` / `'makcu'` / `'arduino'` / `'ddxoft'` / `'xbox'` |
| `inference_backend` | `model.backend` | `'auto'` / `'cuda'` / `'directml'` / `'tensorrt'` / `'cpu'` |
| `cuda_io_binding_enabled` | `performance.cuda_io_binding_enabled` | Zero-copy CUDA inference |
| `makcu_disengage_delay` | `hardware.makcu.disengage_delay` | Seconds aim stays active after releasing aim button (0–20 s) |
| `makcu_aim_button` | `hardware.makcu.aim_button` | Which MAKCU button acts as the aim trigger |
| `always_aim` | `aim.always_aim` | Skip aim-key check; aim every frame |
| `keep_detecting` | `aim.keep_detecting` | Run detection even when not aiming |
| `single_target_mode` | `aim.single_target_mode` | Reduce Web ESP/auto-fire/preview box list to the locked target only (applied *after* sticky lock resolves the pick — see Aiming section) |
| `target_priority_mode` | `tracking.target_priority.mode` | `'distance'` / `'confidence'` / `'composite'` |
| `target_priority_confidence_weight` | `tracking.target_priority.confidence_weight` | Weight of confidence vs. distance in `'composite'` mode |
| `detect_semantic_filter_enabled` | `aim.detect_semantic_filter_enabled` | Filter vegetation/vehicle/sign/HUD false positives before target selection |
| `second_inference_mode` | `ocr.mode` | `'off'` / `'v1_ocr'` / `'v2_onnx'` — secondary weapon-detection method |
| `hud_roi_coords` | `ocr.hud_roi_coords` | ROI `"x1,y1,x2,y2"` for the ONNX weapon/attachment HUD scanner |
| `pid_unsafe_mode` | `aim.pid.unsafe_mode` | Let the GUI's Kp ("reaction speed") sliders be dragged up to 1.00 instead of the default 0.0–0.5 safe cap |
| `aim_y_reduce_enabled` | `aim.y_reduce.enabled` | Enable Y-axis recoil suppression |
| `aim_y_reduce_floor` | `aim.y_reduce.floor` | Minimum Y reduction factor |
| `aim_y_reduce_ramp` | `aim.y_reduce.ramp` | Frames to ramp up suppression |
| `aim_y_reduce_settle_px` | `aim.y_reduce.settle_px` | Pixel threshold to consider recoil settled |
| `aim_y_vel_restore_px_s` | `aim.y_reduce.vel_restore_px_s` | Velocity threshold to restore Y axis |
| `fov_size` / `fov_height` | `aim.fov_size` / `aim.fov_height` | FOV width/height (px); equal by default (square) — independent values make it a rectangle |
| `fov_circle_filter_enabled` | `aim.fov_circle_filter_enabled` | FOV shape: rectangle (default) vs. ellipse — a circle when width == height |
| `fov_reduce_on_target_enabled` | `aim.fov_reduce.enabled` | Temporarily shrink the FOV while a target is locked, so a random target near the edge can't steal the lock |
| `fov_min_size_pct` | `aim.fov_reduce.min_size_pct` | % of `fov_size`/`fov_height` to shrink to while a target is locked |
| `fov_min_size_duration` | `aim.fov_reduce.duration` | Seconds the shrink holds after acquisition before reverting to the full FOV (0 = holds indefinitely while locked) |
| `sticky_lock_enabled` | `tracking.sticky_lock.enabled` | IOU-based target persistence |
| `prediction_enabled` | `tracking.prediction.enabled` | Velocity/Kalman-based motion prediction |
| `uvc_crop_mode` | `capture.uvc.crop_mode` | `'dynamic'` (recompute crop from live Detection Range every frame) / `'fixed'` (freeze a centered `detect_range_size` square — native DLL crop on v2/NV12, software crop otherwise) |
| `uvc_show_window` | `capture.preview.enabled` | Show live capture preview panel |
| `uvc_always_on_top` | `capture.preview.always_on_top` | Preview panel always on top (applies to the PyQt `PreviewPopOutWindow`) |
| `preview_crop_to_detection` | `capture.preview.crop_to_detection` | Crop preview to detection region |
| `web_esp_enabled` | `web_esp.enabled` | Enable the LAN browser overlay server |
| `web_esp_http_port` / `web_esp_ws_port` | `web_esp.http_port` / `.ws_port` | Static-page and WebSocket ports (WS auto-increments on bind conflict) |
| `web_esp_fps` | `web_esp.fps` | Broadcast tick rate for the Web ESP state snapshot |
| `web_control_enabled` | `web_control.enabled` | Enable the LAN control-plane API (`web_control_server.py`) — distinct from Web ESP, this can mutate state |
| `web_control_port` | `web_control.port` | Port the FastAPI/uvicorn control server listens on |
| `web_control_token` | `web_control.token` | Shared access token required on every request (`X-Axiom-Token` header); auto-generated on first enable |
